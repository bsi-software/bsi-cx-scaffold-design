const {cx, SchemaVersion} = require('@bsi-cx/design-build');

module.exports = cx.design
  .withTitle('Landingpage Scaffold Template')
  .withAuthor('BSI Software AG')
  .withDate('26.11.2021')
  .withPreviewImage(require('./thumbnail.png'));
