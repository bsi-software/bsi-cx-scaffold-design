<>
      <div
        class={description.input}
        /* type="radio" */
        id={props.id}
        /* defaultChecked={props.defaultChecked ? true : false} */
      ></div>
      <div class={description.title} htmlFor={props.id}>
        <div class={description.box}>
          <div class={description.titleText}>
            <h3>{props.subheading}</h3>
            <img class={description.icon} src="/icons/plus.svg" />
          </div>
          <div class={description.text}>
            <p>{props.text}</p>
          </div>
        </div>
      </div>
    </>